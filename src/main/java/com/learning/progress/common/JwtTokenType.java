package com.learning.progress.common;

public enum JwtTokenType {
    AUTH,           // Normal auth token
    EMAIL_CHANGE,   // Email change token
    RESET_PASSWORD, // Reset password token
    INVITE          // Invite token (ví dụ)
}
